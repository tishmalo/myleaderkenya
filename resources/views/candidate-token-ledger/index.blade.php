@extends('layouts.app')
@section('page_title', 'Token Ledger')
@section('content')
<div class="max-w-7xl mx-auto"><h1 class="text-3xl font-semibold mb-8 text-white"><i class="fas fa-list text-emerald-500"></i> Token Ledger</h1><div class="bg-zinc-900 border border-zinc-800 rounded-3xl overflow-hidden"><table class="w-full"><thead class="bg-zinc-950"><tr><th class="px-6 py-4 text-left">Candidate</th><th>Type</th><th>Action</th><th>Calc</th><th>Amount</th><th>Before</th><th>After</th><th>Date</th></tr></thead><tbody class="divide-y divide-zinc-800">@forelse($transactions as $tx)<tr><td class="px-6 py-4">{{ $tx->candidate->name ?? '-' }}</td><td class="text-center">{{ $tx->type }}</td><td class="text-center">{{ $tx->action_label ?: '-' }}</td><td class="text-center">{{ $tx->calculation_type ?: '-' }}</td><td class="text-center">{{ number_format($tx->amount) }}</td><td class="text-center">{{ number_format($tx->balance_before) }}</td><td class="text-center">{{ number_format($tx->balance_after) }}</td><td class="text-center">{{ $tx->created_at->format('M j, H:i') }}</td></tr>@empty<tr><td colspan="8" class="text-center py-12 text-zinc-500">No ledger entries yet.</td></tr>@endforelse</tbody></table></div><div class="mt-8">{{ $transactions->links() }}</div></div>
@endsection
