<div class="space-y-6">
    <a href="{{ $back }}" class="inline-flex items-center gap-2 text-emerald-400"><i class="fas fa-arrow-left"></i> Back to audit history</a>
    <section class="rounded-2xl border border-zinc-800 bg-zinc-900/60 p-6"><div class="grid gap-5 md:grid-cols-3"><div><span class="text-xs uppercase text-zinc-500">Activity</span><p class="mt-1 text-white">{{ $audit->summary ?: str($audit->event)->headline() }}</p></div><div><span class="text-xs uppercase text-zinc-500">Actor</span><p class="mt-1 text-white">{{ $actor }}</p></div><div><span class="text-xs uppercase text-zinc-500">Time</span><p class="mt-1 text-white">{{ $audit->created_at?->format('d M Y, H:i:s') }}</p></div></div></section>
    <div class="grid gap-6 lg:grid-cols-2">@foreach(['old_values'=>'Before','new_values'=>'After'] as $field=>$label)<section class="rounded-2xl border border-zinc-800 bg-zinc-900/60 p-6"><h3 class="mb-4 text-lg font-bold text-white">{{ $label }}</h3><dl class="space-y-3">@forelse(($audit->{$field} ?? []) as $key=>$value)<div class="flex justify-between gap-4 border-b border-zinc-800 pb-3"><dt class="text-zinc-500">{{ str($key)->headline() }}</dt><dd class="break-all text-right text-zinc-200">{{ is_array($value) ? json_encode($value) : $value }}</dd></div>@empty<p class="text-zinc-500">No values recorded.</p>@endforelse</dl></section>@endforeach</div>
</div>
