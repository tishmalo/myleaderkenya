@extends('layouts.app')
@section('page_title', 'Token Purchases')
@section('content')
<div class="max-w-7xl mx-auto"><h1 class="text-3xl font-semibold mb-8 text-white"><i class="fas fa-receipt text-emerald-500"></i> Token Purchases</h1><div class="bg-zinc-900 border border-zinc-800 rounded-3xl overflow-hidden"><table class="w-full"><thead class="bg-zinc-950"><tr><th class="px-6 py-4 text-left">Candidate</th><th>Package</th><th>Tokens</th><th>Amount</th><th>Reference</th><th>Date</th></tr></thead><tbody class="divide-y divide-zinc-800">@forelse($purchases as $purchase)<tr><td class="px-6 py-4">{{ $purchase->candidate->name ?? '-' }}</td><td class="text-center">{{ $purchase->package_name }}</td><td class="text-center">{{ number_format($purchase->token_amount) }}</td><td class="text-center">{{ $purchase->currency }} {{ number_format($purchase->price) }}</td><td class="text-center">{{ $purchase->payment_reference ?: '-' }}</td><td class="text-center">{{ $purchase->created_at->format('M j, Y') }}</td></tr>@empty<tr><td colspan="6" class="text-center py-12 text-zinc-500">No purchases yet.</td></tr>@endforelse</tbody></table></div><div class="mt-8">{{ $purchases->links() }}</div></div>
@endsection
